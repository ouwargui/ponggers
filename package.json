{
  "name": "ponggers",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --workspaces --if-present --parallel dev",
    "lint": "bun --workspaces --if-present lint",
    "format": "bun --workspaces --if-present format"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
